# Git Workflow

- **Always use feature branches:** Never commit or push directly to the `main` branch. 
- **Naming:** For every new task, create a branch named `feature/<name>` or `fix/<name>`.
- **Process:** Make all commits on the isolated branch, push the branch to GitHub, and instruct the user to open a Pull Request.
