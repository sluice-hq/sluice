# Database connection details are supplied by the disposable Testcontainers PostgreSQL instance.
# Keep external listeners and storage setup disabled in the default integration-test profile.
spring.rabbitmq.listener.simple.auto-startup=false
azure.storage.configure-cors=false
